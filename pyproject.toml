[project]
name = ""
version = "0.1.0"
description = "
readme = "README.md"
requires-python = ">=3.13.2"
dependencies = [
    "torch>=2.12.1",
    "tqdm",
    "sklearn",
    "matplotlib",
    "pandas",
    "scipy",
    "optuna"
]